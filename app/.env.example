# Base URL of the KubeStream backend. Leave unset (or empty) to use relative
# requests, which work in dev via the Vite proxy and in any same-origin
# deployment. Set this when the frontend is served separately from the
# backend (e.g. a static build behind a CDN, or a different host/port).
VITE_SERVER_URL=http://localhost:4000
